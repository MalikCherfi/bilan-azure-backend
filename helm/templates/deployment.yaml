apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-app
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: back
      app.kubernetes.io/instance: back-app
      app.kubernetes.io/component: backend
      app.kubernetes.io/part-of: back-app
      app.kubernetes.io/managed-by: Helm
  template:
    metadata:
      labels:
        app.kubernetes.io/name: back
        app.kubernetes.io/instance: back-app
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: back-app
        app.kubernetes.io/managed-by: Helm
    spec:
      containers:
        - name: back-container
          image: '{{ .Values.acrName }}.azurecr.io/back:{{ .Values.imageTag }}'
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "aks"

            # PostgreSQL connection
            - name: DB_HOST
              value: {{ .Values.db.host | quote }}
            - name: DB_NAME
              value: {{ .Values.db.name | quote }}
            - name: DB_USERNAME
              value: {{ .Values.db.user | quote }}
            - name: DB_PASSWORD
              value: {{ .Values.db.password | quote }}

            # Redis Cache connection
            - name: REDIS_HOSTNAME
              value: {{ .Values.redis.host | quote }}
            - name: REDIS_PORT
              value: {{ .Values.redis.port | quote }}
            - name: REDIS_PASSWORD
              value: {{ .Values.redis.password | quote }}
            - name: REDIS_SSL_ENABLED
              value: {{ .Values.redis.sslEnabled | quote }}

            # API key for backend security
            - name: BACKEND_API_KEY
              value: {{ .Values.app.apiKey | quote }}

            # Azure blob storage configuration
            - name: STORAGE_ACCOUNT_NAME
              value: {{ .Values.app.storage.accountName | quote }}
            - name: STORAGE_CONTAINER_NAME
              value: {{ .Values.app.storage.containerName | quote }}
            - name: STORAGE_SAS_TOKEN
              value: {{ .Values.app.storage.sasToken | quote }}  
          resources:
            requests:
              cpu: '200m'
              memory: '256Mi'
            limits:
              cpu: '500m'
              memory: '512Mi'