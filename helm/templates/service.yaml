apiVersion: v1
kind: Service
metadata:
  name: back-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/name: back
    app.kubernetes.io/instance: back-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP